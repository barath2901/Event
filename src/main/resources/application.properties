spring.application.name=EventPortal

# --- DATABASE CONFIGURATION ---
# Corrected URL: Clean JDBC format without the duplicate string
spring.datasource.url=jdbc:mysql://shinkansen.proxy.rlwy.net:31107/railway?allowPublicKeyRetrieval=true&useSSL=false

# Credentials
spring.datasource.username=root
spring.datasource.password=hzqoxEblChEeIdaAqlooVyiSxlORzFws

# --- DRIVER & DIALECT ---
# MySQL Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# FIXED: Changed from PostgreSQLDialect to MySQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Create/Update tables automatically
spring.jpa.hibernate.ddl-auto=update